# Host path mounted read-only at /books by compose.yaml.
MOTH_BOOKS_PATH=./books

# Keep false for local HTTP. Set true when HTTPS is terminated by a reverse proxy.
MOTH_COOKIE_SECURE=false
MOTH_SESSION_TTL_DAYS=30
MOTH_LOG=info
