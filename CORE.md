# Moth 核心定义

Moth 是给自己使用的单用户、自托管 PWA 阅读器。它把一个只读文件夹映射成书库，不接管文件、不复制文件、不改名文件。

## 只承诺三件事

1. 在线 Web/PWA 阅读 EPUB、TXT、CBZ 和经典无 DRM MOBI。
2. 在服务器保存跨设备阅读位置。
3. 在手机浏览器和已安装 PWA 中保持同一套阅读体验。

## 数据边界

- 唯一书库根目录来自 `MOTH_BOOKS_DIR`，子目录就是分类。
- 源文件只读；SQLite、封面、TXT UTF-8 派生缓存和 MOBI→EPUB 缓存只写入 `MOTH_DATA_DIR`。
- 数据库是可重建索引。旧 schema 不迁移，备份后删除 `moth.db`、WAL/SHM，再重新扫描。
- 单用户认证由 Moth 自己负责；反向代理负责 HTTPS 和网络暴露。

## 阅读模型

- EPUB：浏览器 Range 读取原始 EPUB，保存 CFI；目录来自 EPUB 自己的 TOC。
- TXT：服务端检测编码并按中文规则建立虚拟章节；客户端通过轻量 Foliate adapter 使用与 EPUB 相同的目录、字号、行距、主题和分页/滚动外壳。定位为章节索引、UTF-16 字符偏移和编码。
- CBZ：自然排序的 ZIP 页面；支持单页、双页、Webtoon、LTR/RTL 和屏幕适配。定位为页码、页内进度和总体进度。
- MOBI：仅作为输入格式，首次打开时转换成数据目录中的 EPUB 缓存，然后进入 EPUB reader。

## UI 约束

首页只显示继续阅读和目录预览。目录遵循固定的根目录/分类/系列/作品层级，每个分类首页最多展示四个系列，点击后仍可进入完整目录树。

不做全局搜索、作者/格式筛选、小说/漫画栏目、手工系列或文件整理。目录树承担找书和分类职责。

阅读器在手机上使用触摸翻页；章节和 CBZ 页码通过标题栏抽屉选择，字号和行距设置默认收起并跟随首页主题。

## 明确不做

离线书籍、IndexedDB 进度队列、多用户、OPDS、Kobo/KOReader、上传、在线书源、元数据抓取、AI、推荐、书评、自动改名/复制、AZW3/KF8、PDF、FB2。

## 完成标准

- 四种输入都能扫描、显示封面/标题并进入正确 reader。
- EPUB/TXT 能通过目录跳转并恢复精确位置；CBZ 能恢复页和页内位置。
- 关闭 reader 或切后台时最后一次进度不会丢失。
- 扫描状态真实可见，扫描不完整时不 prune 索引。
- PWA 只缓存 shell 和构建资源，不缓存认证、API、章节、文件或漫画页。
